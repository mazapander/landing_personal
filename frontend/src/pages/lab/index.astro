---
import { getCollection } from 'astro:content'
import BaseLayout from '@/layouts/BaseLayout.astro'
import { labItems } from '@/lib/lab-items.mjs'

interface LabItem { id: string; title: string; description: string; status: string; href: string }
const items = labItems(await getCollection('projects')) as LabItem[]
---

<BaseLayout title="Lab — Ander Fernández" description="Productos y experimentos públicos de datos, IA y software.">
  <div class="practice-page">
    <header class="practice-hero">
      <p class="practice-eyebrow">Lab</p>
      <h1>Productos y experimentos que se pueden explorar.</h1>
      <p>Una selección de trabajo público para comprobar cómo conecto producto, datos, IA y software más allá de una descripción.</p>
      <nav class="page-context" aria-label="Explorar el Lab"><a href="/proyectos/">Casos de estudio <span aria-hidden="true">→</span></a><a href="/notas/">Notas <span aria-hidden="true">→</span></a></nav>
    </header>
    <section class="lab-grid" aria-label="Productos públicos">
      {items.map((item) => <article>
        <p class="practice-eyebrow">{item.status}</p>
        <h2>{item.title}</h2>
        <p>{item.description}</p>
        <a href={item.href} target="_blank" rel="noreferrer" data-track-event="demo_open" data-track-location="lab" data-track-project={item.id}>Explorar <span aria-hidden="true">→</span></a>
      </article>)}
    </section>
  </div>
</BaseLayout>
